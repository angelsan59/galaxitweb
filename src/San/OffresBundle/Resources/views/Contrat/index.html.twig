
{% extends "SanAdminBundle::layout.html.twig" %}

{% block title %}
  Accueil contrats - {{ parent() }}
{% endblock %}

{% block body %}

  <h3 class="bgblue">Liste des contrats</h3>
  {% for message in app.session.flashbag.get('info') %}
        <div class="alert alert-info">{{ message }}</div>
      {% endfor %}
<div class="well">
   <a href="{{ path('san_contrats_add') }}" role="button" class="btn btn-success btn-xs"><i class="fa fa-plus"></i> Ajouter un contrat</a>
    <table class="table table-striped">
    <thead>
      <tr>
        <th>Titre du contrat</th>
        <th></th>
        <th>Description</th>
        </tr>
    </thead>
    <tbody>
    {% for contrat in listContrats %}
     <tr>
        <td>{{ contrat.nom }}</td>
        <td><a href="{{ path('san_contrats_edit', {'id': contrat.id}) }}" role="button" class="btn btn-info btn-xs"><i class="glyphicon glyphicon-edit"></i> Modifier</a>
        <a href="{{ path('san_contrats_delete', {'id': contrat.id}) }}" role="button" class="btn btn-danger btn-xs"><i class="glyphicon glyphicon-trash"></i> Supprimer</a></td>
        
     <td>{{ contrat.content }}</td>
        
      </tr>
      {% endfor %}
    </tbody>
  </table>
      
</div>
  <ul class="pagination">
    {# On utilise la fonction range(a, b) qui crée un tableau de valeurs entre a et b #}
    {% for p in range(1, nbPages) %}
      <li{% if p == page %} class="active"{% endif %}>
        <a href="{{ path('san_contrats_homepage', {'page': p}) }}">{{ p }}</a>
      </li>
    {% endfor %}
  </ul>

{% endblock %}
