
{% extends "SanAdminBundle::layout.html.twig" %}

{% block title %}
  Modifier un contrat - {{ parent() }}
{% endblock %}

{% block body %}

  <h3 class="bgblue">Modifier un contrat</h3>
{% for message in app.session.flashbag.get('info') %}
        <div class="alert alert-info">{{ message }}</div>
      {% endfor %}
  {{ include("SanOffresBundle:Contrat:form.html.twig") }}


  <p>
    <a href="{{ path('san_contrats_homepage') }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la liste
    </a>
  </p>

{% endblock %}