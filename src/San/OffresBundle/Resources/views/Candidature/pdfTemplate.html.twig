<html>
  <head>
     
    <style>
      /* On commence par définir les propriétés de la page : les marges en haut
       * et en bas permettront d'insérer un en-tête et un pied de page */
      @page {
        margin: 50px 0 25px 0;
      }
      /* La position fixed permet de placer les éléments sur toutes les pages
       * du document pdf, le reste n'est que décoration */
      #header, #footer {
        position: fixed; 
        left: 0px;
        right: 0px;
        color: #fff;
        background-color: #000;
      }
      #header {
        top: -50px;
        height: 50px;
      }
      #footer {
        bottom: -25px;
        height: 25px;
      }
      /* Pour améliorer un peu la lisibilité on ajoute une marge au corps de
       * texte. Ca aurait pu être fait en agrandissant les marges au niveau
       * de la page mais je trouve qu'utiliser un div dédié pour le contenu
       * est cohérent */
      #content { 
        margin: 25px;
      }
      /* On teste quelques d'options css différentes */
      h1 { text-transform: uppercase; text-align: center; font-weight: bold}
      h2 {font-style: italic; font-weight: bold; border: solid 1px #ccc; }
      /* On met même en place un système de colonnes pour tester les float */
      .col-25, .col-75 { float: left; }
      .col-25 { width: 25%; }
      .col-75 { width: 75%; }
      .row { clear: both; }
    </style>
  </head>
  <body>
    <!-- On commence par ajouter le header et le footer pour qu'ils soient 
      visibles sur toutes les pages. Si on les ajoute après avoir rempli plus
      d'une page, ils ne seront insérés qu'après le saut de page -->
    <div id="header">
      <!-- Grâce aux options définies dans le contrôleur on peut insérer des 
        images à partir de leur url absolue -->
      <img src="{{ app.request.getSchemeAndHttpHost() ~ asset('img/logoclear.png') }}" alt="logo"width="200px">
    </div>
    <div id="footer">Texte du footer</div>
    <!-- Maintenant on rempli le document -->        
    <div id="content">
      <h1>{{candidature.offre.titre}}</h1>
      <h2>{{candidature.content}}</h2>
      <div class="row">
        
          <div class="col-sm-12">
    <h3 class="section-title">{{ candidature.user.prenom }} {{ candidature.user.nom }}</h3>
        <p class="section-description">Candidature à l'offre {{ candidature.offre.titre }} - Disponible le {{ candidature.dateDispo|date('d/m/Y') }}</p>
          </div>
           
     </div>
  <div class="well">
     <p class="section-description">{{ candidature.content }}</p>
     <p><a href="{{ vich_uploader_asset(candidature, 'cvFile') }}" target="_blank" class="btn btn-info"><i class="fa fa-file-pdf-o" style="font-size:18px;color:red;"></i> Voir le cv</a></p>
  </div>
     
    <div class="panel panel-primary">
  <div class="panel-heading">Coordonnées</div>
  <div class="panel-body"> 
      <div class="col-sm-6">
          <b>Adresse</b> : {{ candidature.adresse }} {{ candidature.cp }} {{ candidature.ville }} {{ candidature.pays }}<br/>
          <b>Téléphone</b> : {{ candidature.user.telephone }}<br/>
          <b>Portable</b> : {{ candidature.user.portable }}</div>
      <div class="col-sm-6">
      <ul>
          <li><b>Web</b> : {{ candidature.web }}</li>
      <li><b>Linkdn</b> : {{ candidature.linkdn }}</li>
      <li><b>Viadeo</b> : {{ candidature.viadeo }}</li>
      <li><b>Twitter</b> : {{ candidature.twitter }}</li>
      </ul></div></div>
    </div>       
    
       <div class="panel panel-primary">
  <div class="panel-heading">Catégories et compétences</div>
  <div class="panel-body">
      <div class="col-sm-6">
     {% if not candidature.categories.empty %}
         <b>Catégories </b>
        <ul>
    {% for category in candidature.categories %}
        <li>{{ category.nom }}</li>
    {% endfor %}
      </ul>
      {% endif %}
  </div>
  <div class="col-sm-6">
      {% if not candidature.competences.empty %}
  
          <b>Compétences</b>
             
      <ul>
    {% for competence in candidature.competences %}
        <li>{{ competence.nom }}</li>
    {% endfor %}
      </ul>
{% endif %}
  </div>
  </div>
  </div>
     



    <div class="panel panel-primary">
  <div class="panel-heading">Formation</div>
  <div class="panel-body">{{ candidature.formation }}</div>
    </div> 
    <div class="panel panel-primary">
  <div class="panel-heading">Réalisations</div>
  <div class="panel-body">{{ candidature.realisations }}</div>
    </div>
    <div class="panel panel-primary">
  <div class="panel-heading">Technologies</div>
  <div class="panel-body">{{ candidature.techno }}</div>
    </div>   
    <div class="panel panel-primary">
  <div class="panel-heading">Evolution</div>
  <div class="panel-body">{{ candidature.evolution }}</div>
    </div>   
    </div>       
  </body>
</html>