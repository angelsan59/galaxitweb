{% for message in app.session.flashbag.get('info') %}
        <div class="alert alert-info">Message flash : {{ message }}</div>
      {% endfor %}
{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
    <h4 class="text-center"><i class="fa fa-user"></i> Bienvenue {{ app.user.username }}</h4>
    
    <p class="section-description">Si ce n'est pas vous, <a href="{{ path('fos_user_security_logout') }}">déconnectez-vous</a></p>
    <div class="section-title-divider"></div>
    <div class="col-md-12 text-center">

{% else %}
    
        <p class="section-description"> Vous devez d'abord vous inscrire avant de postuler.</p>
        <p class="section-description"> Déjà inscrit? Loggez-vous ! {{ render(controller("FOSUserBundle:Security:login")) }}</p>   
 {% endif %} </div> 
 
  {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}

  {# Les erreurs générales du formulaire. #}
  {{ form_errors(form) }}
 <div class="container">
 <div class="row">
      <div class="col-sm-7">
    {{ form_row(form.content) }}
    <div class="panel panel-primary">
        <div class="panel-heading"><b>Coordonnées</b></div>
  <div class="panel-body"> 
      <div class="candform">{{ form_row(form.adresse) }}
    {{ form_row(form.ville) }}
    {{ form_row(form.cp) }}
    {{ form_row(form.pays) }}
      </div></div>
</div>
   
   <div class="panel panel-primary">
        <div class="panel-heading"><b>Web et Media</b></div>
  <div class="panel-body"> 
      <div class="candform"> {{ form_row(form.web) }}
    {{ form_row(form.linkdn) }}
    {{ form_row(form.viadeo) }}
    {{ form_row(form.twitter) }}
      </div></div>
</div>
    
      <div class="panel panel-primary">
        <div class="panel-heading"><b>Fiche "Profil Candidat"</b></div>
  <div class="panel-body"> 
      <div class="candform">  
          <p>Galax-IT demande à tous les candidats de répondre aux questions 
              suivantes afin d'élaborer votre profil candidat. </p>        
    {{ form_row(form.formation) }}
    {{ form_row(form.realisations) }}
    {{ form_row(form.techno) }}
    {{ form_row(form.evolution) }}
      </div></div>
</div>
    </div>
    <div class="col-sm-4 col-sm-offset-1">
        
          {{ form_rest(form) }}
         
   {{ form_row(form.competences) }}
    {{ form_end(form) }}
    </div></div></div>
