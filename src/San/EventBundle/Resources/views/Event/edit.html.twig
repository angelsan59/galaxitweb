
{% extends "SanAdminBundle::layout.html.twig" %}

{% block title %}
  Modifier un evenement - {{ parent() }}
{% endblock %}

{% block body %}

  <h3 class="bgblue">Modifier un évènement</h3>
{% for message in app.session.flashbag.get('info') %}
        <div class="alert alert-success">{{ message }}</div>
      {% endfor %}
  {{ include("SanEventBundle:Event:form.html.twig") }}


  <p>
    <a href="{{ path('san_event_homepage') }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la liste
    </a>
  </p>

{% endblock %}