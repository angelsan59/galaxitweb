
{% extends "SanAdminBundle::layout.html.twig" %}

{% block title %}
  Modifier un statut - {{ parent() }}
{% endblock %}

{% block body %}

  <h3 class="bgblue">Modifier un statut</h3>
{% for message in app.session.flashbag.get('info') %}
        <div class="alert alert-info">{{ message }}</div>
      {% endfor %}
  {{ include("SanUserBundle:Statut:form.html.twig") }}


  <p>
    <a href="{{ path('san_statuts_homepage') }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la liste
    </a>
  </p>

{% endblock %}